import styled from 'styled-components';
import { theme } from '../theme';

export const Title = styled.h1`
  margin-bottom: ${theme.rhythm(4.5)};
`;

export const SpinnerContainer = styled.div`
  margin-top: 3rem;
  margin-left: auto;
  margin-right: auto;
`;
